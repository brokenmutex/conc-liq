# Contracts

This phase is deliberately read-only and contains no deployable contract.
Foundry is configured now so later transaction-building work can be checked
against Solidity and fork tests before any signer is introduced.
